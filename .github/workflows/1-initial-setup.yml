name: 1. Build Base Kali Image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Maximize Runner Storage (Purge Pre-installed SDKs)
        run: |
          echo "Purging unnecessary host environments to give the Docker builder maximum breathing room..."
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          df -h

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Base Image
        uses: docker/build-push-action@v5
        with:
          context: ./.kali-lab
          file: ./.kali-lab/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/kali-study-lab:latest
